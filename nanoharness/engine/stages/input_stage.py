# InputStage: 接收并规范化用户输入 / InputStage: receive and normalize user input
# 处理附件（图片/文件）引用 / Handle attachment (image/file) references
# 注入 extra_context（通道信息 / 用户画像） / Inject extra_context (channel info / user profile)
