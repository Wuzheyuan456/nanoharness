# ProviderStage: 解析并初始化本次 turn 使用的 LLM Provider / ProviderStage: parse and initialize the LLM Provider used for this turn
# 根据 Router 决策选择对应 tier 的 provider 实例 / Select the provider instance of the corresponding tier based on Router decision
# 构建工具定义列表（tool_definitions） / Build the tool definition list (tool_definitions)
