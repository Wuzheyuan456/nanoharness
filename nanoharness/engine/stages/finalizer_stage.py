# TurnFinalizerStage: turn 收尾 / TurnFinalizerStage: turn finalization
# 写入 transcript（session 持久化） / Write transcript (session persistence)
# 触发 memory 同步（MemoryHook.sync_turn） / Trigger memory sync (MemoryHook.sync_turn)
# 计算并记录 cost / token 用量 / Calculate and record cost / token usage
