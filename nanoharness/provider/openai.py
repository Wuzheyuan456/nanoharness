# OpenAI provider 实现 / OpenAI provider implementation
# OpenAIProvider: 实现 LLMProvider Protocol / OpenAIProvider: implements the LLMProvider Protocol
# 支持流式输出（stream=True） / Supports streaming output (stream=True)
# 支持 function_calling / tool_calls 消息格式 / Supports function_calling / tool_calls message format
